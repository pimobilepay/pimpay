"use client";
import { useEffect, useState } from "react";
export default function AdminFlightsPage() { const [data, setData] = useState<any>({ bookings: [], stats: {} }); const [query, setQuery] = useState(""); useEffect(() => { const timer = setTimeout(() => fetch(`/api/admin/flights?query=${encodeURIComponent(query)}`).then(r => r.json()).then(setData), 150); return () => clearTimeout(timer); }, [query]); return <main className="mx-auto max-w-6xl px-5 py-8 text-white"><p className="text-xs font-black uppercase tracking-[0.25em] text-blue-400">Admin</p><h1 className="mt-2 text-3xl font-black">Flight Management</h1><div className="mt-6 grid gap-3 sm:grid-cols-4">{[["Total bookings",data.stats.total],["Confirmed",data.stats.confirmed],["Cancelled",data.stats.cancelled],["Volume",data.stats.volume]].map(([label,value]) => <div key={label as string} className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><p className="text-[10px] uppercase text-slate-500">{label}</p><p className="mt-2 text-2xl font-black">{value ?? 0}</p></div>)}</div><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search reference, provider, status or user" className="mt-6 w-full rounded-xl border border-white/10 bg-white/[0.04] px-4 py-3 text-sm outline-none placeholder:text-slate-600" /><div className="mt-4 flex flex-col gap-3">{data.bookings?.map((booking: any) => <div key={booking.id} className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="flex justify-between gap-4"><div><p className="font-black">{booking.bookingReference ?? booking.id}</p><p className="text-xs text-slate-500">{booking.user?.email ?? booking.userId} · {booking.provider}</p></div><span className="text-xs text-blue-300">{booking.status}</span></div><p className="mt-3 text-sm text-slate-400">{booking.totalAmount} {booking.currency} · refund {booking.refundAmount ?? 0}</p></div>)}</div></main>; }
